{
  "name": "gate402",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd apps/server && npm run dev\" \"cd apps/dashboard && npm run dev\"",
    "build": "cd apps/server && npm run build && cd ../dashboard && npm run build",
    "db:studio": "cd packages/db && npx prisma studio"
  },
  "dependencies": {
    "bullmq": "^5.76.8",
    "gate402-agent": "^0.1.4",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.7.0"
  }
}
